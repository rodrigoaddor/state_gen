name: state_gen_example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^4.1.2
  shared_preferences: ^0.5.7+3

dev_dependencies:
  build_runner: ^1.10.0
  state_gen:
    path: ../

flutter:
  uses-material-design: true
